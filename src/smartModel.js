var smartModel = (function() {
	var MODEL = {
		reviews: [],
		rects:   [],
	};

  MODEL.init = function init(data) {
    // data.forEach(function(d) {
    //   MODEL.reviews.push(d);
    // })
    // console.log(MODEL.reviews)
  }

	})()

